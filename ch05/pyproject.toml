[project]
name = "ch05-subagents"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.15.0",
    "deepagents>=0.7.6",
    "langchain-openai>=1.5.1",
    "tavily-python>=0.7.27",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
